apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: backend-deploy
  name: backend-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-deploy
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: backend-deploy
    spec:
      containers:
      - image: redis:latest
        name: redis
        command: ["/bin/bash", "-c", "env","sleep 3600"]
        envFrom:
          - secretRef:
              name: secretfromfile

status: {}
